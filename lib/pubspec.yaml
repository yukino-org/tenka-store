name: extensions_store
description: Extensions store of Yukino.
version: 0.0.0

publish_to: none

environment:
    sdk: ">=2.12.0 <3.0.0"

dependencies:
    extensions:
        git:
            url: git://github.com/yukino-org/packages.git
            ref: extensions
    extensions_dev_tools:
        git:
            url: git://github.com/yukino-org/packages.git
            ref: extensions_dev_tools
    http: ^0.13.4
    path: ^1.8.1
    utilx:
        git:
            url: git://github.com/yukino-org/packages.git
            ref: utilx
    yaml: ^3.1.0

dev_dependencies:
    devx:
        git:
            url: git://github.com/yukino-org/packages.git
            ref: devx
    lint: ^1.5.3
