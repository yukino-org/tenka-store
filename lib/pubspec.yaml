name: tenka_store
description: "🏪 Where all Tenka modules come together."
version: 0.0.0

publish_to: none

environment:
    sdk: ">=2.12.0 <3.0.0"

dependencies:
    http: ^0.13.4
    path: ^1.8.1
    tenka:
        git:
            url: git://github.com/yukino-org/packages.git
            ref: dart_tenka
    tenka_dev_tools:
        git:
            url: git://github.com/yukino-org/packages.git
            ref: dart_tenka_dev_tools
    utilx:
        git:
            url: git://github.com/yukino-org/packages.git
            ref: dart_utilx
    yaml: ^3.1.0

dev_dependencies:
    devx:
        git:
            url: git://github.com/yukino-org/packages.git
            ref: dart_devx
